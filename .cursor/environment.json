{
  "user": "ubuntu",
  "build": {
    "dockerfile": ".cursor/Dockerfile.cursor-cloud-agent",
    "context": "."
  },
  "install": "cargo fetch && cargo build --tests && bash scripts/ensure-localstack.sh",
  "start": "bash scripts/ensure-localstack.sh"
}
